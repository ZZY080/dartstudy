/* 

关键字为with。

作为mixis的类只能继承自object，不能继承其它类。
作为mixis的类不能有构造函数。
一个类可以mixins多个mixins类。
mixins绝不是继承，也不是接口，而是一种全新的特性。
 */
import 'dart:core';
// 暂时不支持
// class Mark {
//   String name = "kenny";
//   void printInfo() {
//     print(this.name);
//   }
// }

// class Person with Mark {}

// void main() {
//   Person().printInfo();
//   print(Person().name);
// }
